#
# example 'greetd' config.toml - when using 'tuigreet'
#
[terminal]
vt = "7"

[default_session]
command = "/usr/sbin/tuigreet --user-menu --user-menu-min-uid 1000 --remember --remember-session --time --issue --asterisks"
user = "greetd"

# End of file
